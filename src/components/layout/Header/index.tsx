import { MainNavbar } from "../MainNavbar";
import "./styles.scss";

const Header = () => {
  return (
    <header className="main-header">
      <MainNavbar />
    </header>
  );
};

export { Header };
