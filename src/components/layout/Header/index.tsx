import { MainNavbar } from "../MainNavbar";
import "./styles.scss";
import React from "react";

const Header = () => {
  return (
    <header className="main-header">
      <MainNavbar />
    </header>
  );
};

export { Header };
